# Robocon Website

## Setup instruction

1. Deprecate `date-fns`
    ```bash
    npm install date-fns@3.0.0
    ```
1. Install dependecies
    ```bash
    npm install
    ```
1. Run the server
    ```
    npm run dev
    ```